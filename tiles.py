import math

tiles = int(input("how many tiles?: "))

side_length = math.sqrt(tiles)
side_length = math.floor(tiles)

if side_length** <= tiles**:
    print("The largest square has side length (side_length)")
